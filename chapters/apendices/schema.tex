\chapter{Schema} \label{app:schema}
schema, extractor in readeable form (maybe only one of them), mention that they are not always the same because we generated training set and testing set per signature